{
  "name": "durga-puja-platform-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
